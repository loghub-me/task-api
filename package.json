{
  "name": "loghub-me-task-api",
  "version": "0.1.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch ./src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile server ./src/index.ts"
  },
  "dependencies": {
    "@elysiajs/bearer": "^1.3.0",
    "@elysiajs/cors": "^1.3.3",
    "elysia": "latest",
    "loghub-markdown-renderer": "^0.1.0",
    "pino": "^9.8.0",
    "sharp": "^0.34.2",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.5.3"
  },
  "module": "src/index.js"
}
